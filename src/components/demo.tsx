import React, { useEffect, useReducer, useState } from 'react'



const Templatedemo = () => {    
  return (
    <>
        {/* Số là {count}

        <button type="button" onClick={()=>{setCount({type:'cong'})}}>CỘng</button>
        <button type="button" onClick={()=>{setCount({type:'tru'})}}>Trừ</button>
        <button type="button" onClick={()=>{setCount({type:'reset'})}}>Reset</button> */}
    </>
  )
}

export default Templatedemo